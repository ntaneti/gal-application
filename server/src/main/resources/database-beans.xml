<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:jpa="http://www.springframework.org/schema/data/jpa"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd
                           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd
                           http://www.springframework.org/schema/data/jpa http://www.springframework.org/schema/data/jpa/spring-jpa.xsd">

    <!--
        API schema configuration
    -->

    <!-- Processes common spring annotations -->
    <context:annotation-config />

    <!-- Scans for @Transactional to define transaction boundaries -->
    <tx:annotation-driven transaction-manager="transactionManager"/>

    <!-- Scan to pick up the Spring DAOs -->
    <context:component-scan base-package="org.familysearch.gal.application.dal.**" />

    <!-- Translates Hibernate exceptions to Spring exceptions for @Repository (DAO) classes -->
    <bean class="org.springframework.dao.annotation.PersistenceExceptionTranslationPostProcessor" />

       <!-- Flyway Configuration -->
    <bean id="flyway" class="com.googlecode.flyway.core.Flyway" init-method="migrate"
        p:dataSource-ref="dataSourceMessage"
        p:locations="classpath:flyway/migration/appgallery" />


    <!-- JPA configuration -->
    <bean id="jpaVendorAdapter" class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter"
        p:database="MYSQL"
        p:showSql="false"
        p:generateDdl="false"
        p:databasePlatform="org.hibernate.dialect.MySQL5Dialect" />

    <!-- JPA Dialect -->
    <bean id="jpaDialect" class="org.familysearch.gal.application.dal.api.base.ValidatingHibernateJPADialect" />

    <!-- EntityManager factory -->
    <bean id="entityManagerFactory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean" depends-on="flyway"
        p:dataSource-ref="dataSourceMessage"
        p:jpaVendorAdapter-ref="jpaVendorAdapter"
        p:jpaDialect-ref="jpaDialect"
        p:persistenceUnitName="appGalleryPU" />

    <!-- TransactionMangaer -->
    <bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager"
        p:entityManagerFactory-ref="entityManagerFactory"
        p:jpaDialect-ref="jpaDialect" />

    <bean id="dataSourceMessage" class="org.apache.commons.dbcp.BasicDataSource"
          p:driverClassName="com.mysql.jdbc.Driver"
          p:url="${gallery.db.url}"
          p:username="${gallery.db.username}"
          p:password="${gallery.db.password}"
          p:initialSize="40"
          p:maxActive="100"
          p:validationQuery="select 1"
          p:testOnBorrow="true"/>
          
	<jpa:repositories base-package="org.familysearch.gal.application.dal.api.**"
                      entity-manager-factory-ref="entityManagerFactory"
                      transaction-manager-ref="transactionManager" />
                      
    <beans profile="cloud-blueprint">

        <bean id="sasObjectRequester" class="org.familysearch.sas.client.ObjectRequester"/>

        <bean id="dataSourceMessage" class="org.familysearch.engage.foundation.dbcp.SasEnabledBasicDataSource"
              p:driverClassName="com.mysql.jdbc.Driver"
              p:objectRequester-ref="sasObjectRequester"
              p:sasObjectName="${MESSAGE_RDS_SAS_OBJECT_NAME}"
              p:url="${gallery.db.url}"
              p:initialSize="40"
              p:maxActive="100"
              p:validationQuery="select 1"
              p:testOnBorrow="true"/>
    </beans>
</beans>